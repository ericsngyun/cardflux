# CardFlux CI/CD Pipeline Dependencies
# Minimal subset for GitHub Actions scraper (no desktop app, no OCR)
# This file dramatically reduces installation time and disk usage

# Core ML/AI Framework (CPU-only for embeddings)
torch>=2.1.0
torchvision>=0.16.0

# Vision Transformers (for DINOv2 embeddings)
transformers>=4.35.0

# Vector Search (CPU-only, no GPU needed in CI)
faiss-cpu>=1.7.4

# Image Processing (for card detection and preprocessing)
Pillow>=10.1.0
opencv-python>=4.8.0

# Numerical Computing
numpy>=1.24.0

# Progress bars (for user feedback)
tqdm>=4.66.0

# EXCLUDED from CI (not needed for scraping):
# ❌ easyocr - Large download, OCR not used in CI scraping
# ❌ paddleocr - Alternative OCR, not needed
# ❌ pytest/testing - Tests run separately, not during scraping
# ❌ faiss-gpu - No GPU in CI runners, CPU is sufficient

# Installation size comparison:
# - Full requirements.txt: ~3-5 GB (includes CUDA, OCR models)
# - requirements-ci.txt: ~800 MB (minimal, scraper-only)
# - Savings: 75%+ smaller, 3-5x faster install

# Usage:
# Local development: pip install -r requirements.txt
# GitHub Actions: pip install -r requirements-ci.txt
